import { redirect } from "next/navigation"

export default function ZonesInterventionIndex() {
  // La redirection reste la même, car il s'agit d'un chemin interne
  redirect("/zones-intervention")
}
